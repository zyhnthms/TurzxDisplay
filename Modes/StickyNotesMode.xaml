<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="TurzxDisplay.Modes.StickyNotesMode"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:TurzxDisplay.Controls"
    Width="800" Height="480">

    <Grid Background="#E9EFF7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="34,26,0,4">
            <TextBlock Text="便签" FontSize="24" FontWeight="SemiBold" Foreground="#2E3D59"
                       FontFamily="Segoe UI Variable Display" VerticalAlignment="Center"/>
            <Border CornerRadius="10" Background="#DFE7F3" Padding="10,3" VerticalAlignment="Center">
                <TextBlock x:Name="CountText" FontSize="15" Foreground="#7687A3"
                           FontFamily="Segoe UI Variable Display"/>
            </Border>
        </StackPanel>

        <Grid x:Name="NotesGrid" Grid.Row="1" Margin="24,10,24,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        </Grid>
    </Grid>
</UserControl>
